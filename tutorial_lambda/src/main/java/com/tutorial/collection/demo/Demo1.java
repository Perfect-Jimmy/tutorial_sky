package com.tutorial.collection.demo;

import java.util.List;
import java.util.Optional;
import java.util.stream.Collectors;

/**
 * @author jimmy
 * @description
 * @date 2020/4/13 18:06
 */
public class Demo1 {
    public static void main(String[] args) {
     //   List<String> fields = (List<String>) values.stream().map(v -> Optional.ofNullable(v).map(val -> String.valueOf(val)).orElse(null)).collect(Collectors.toList());

    }
}
